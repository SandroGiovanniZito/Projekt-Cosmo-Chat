const joinButton = document.getElementById("join-button");
joinButton.addEventListener("onclick", joinMeeting);

socket = io() {
    joinMeeting.getElementById("join-button").addEventListener("onclick", )
}

function joinMeeting() {
  const joinMeeting = "Chat\public\chatroom.html"
}

function message (){
    if (message == ''.value);
    return;
}